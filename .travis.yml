dist: buster

services:
  - docker

language: python

python:
  - 3.6
  - 3.7

install:
  - pip install -r requirements_all.txt

script:
  - make check
  - make test
  - make docker

deploy:
  provider: script
  script: make deploy
  on:
    branch: master
